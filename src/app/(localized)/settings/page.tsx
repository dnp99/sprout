import { AppShell } from "@/components/AppShell";

/** `/settings` — a section route. AppShell renders the matching view for the visible
 *  surface and guards auth. */
export default function Page() {
  return <AppShell />;
}
